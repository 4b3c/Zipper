[Unit]
Description=Refresh Canvas hourly

[Timer]
OnCalendar=hourly
# The keepalive only works if it actually runs; catch up after a reboot rather
# than waiting for the next hour boundary.
Persistent=true
RandomizedDelaySec=120

[Install]
WantedBy=timers.target
